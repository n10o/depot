Under construction

use express scaffold

```
$ cat config.js
var config = {}

config.endPoint = 'xxx';
config.awsId = 'xxx';
config.awsSecret = 'xxx';
config.assocId = 'xxx';

module.exports = config;
```
